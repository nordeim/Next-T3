# Required for NextAuth (used to sign/verify JWTs). Generate securely:
# openssl rand -base64 32
AUTH_SECRET=replace-with-a-long-random-string

# Discord OAuth credentials from https://discord.com/developers/applications
AUTH_DISCORD_ID=replace-with-your-discord-client-id
AUTH_DISCORD_SECRET=replace-with-your-discord-client-secret

# Prisma connection string. For local PostgreSQL via T3 script:
# ./start-database.sh (if provided), then set your connection string:
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/t3_test?schema=public

# Optional (defaults to development). Set explicitly for clarity.
NODE_ENV=development

